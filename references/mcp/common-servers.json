{
  "// NOTE": "Remove all comment lines (keys starting with //) before using — JSON does not support comments.",

  "mcpServers": {
    "github": {
      "// about": "GitHub MCP server for repository operations, issues, PRs, and search.",
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "$COPILOT_MCP_GITHUB_TOKEN"
      }
    },

    "database": {
      "// about": "Database MCP server for querying your development database. Replace with your actual DB server package.",
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-postgres"],
      "env": {
        "POSTGRES_CONNECTION_STRING": "$COPILOT_MCP_DATABASE_URL"
      }
    },

    "cloud": {
      "// about": "Cloud provider MCP server for infrastructure operations. Replace with your provider's package (AWS, Azure, GCP).",
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-aws"],
      "env": {
        "AWS_ACCESS_KEY_ID": "$COPILOT_MCP_AWS_ACCESS_KEY_ID",
        "AWS_SECRET_ACCESS_KEY": "$COPILOT_MCP_AWS_SECRET_ACCESS_KEY",
        "AWS_REGION": "$COPILOT_MCP_AWS_REGION"
      }
    }
  }
}
